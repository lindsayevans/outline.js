jobs:
- job: Beta
  pool:
    vmImage: 'Ubuntu 16.04'

    trigger:
    tags:
        include:
        - '*-beta.*'
    steps:
    - template: deployment-templates/build.yml

- job: Release
  pool:
    vmImage: 'Ubuntu 16.04'

    trigger:
    branches:
        include:
        - master
    steps:
    - template: deployment-templates/build.yml
